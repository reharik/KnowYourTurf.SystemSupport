@model EmailViewModel
@using SystemSupport.Web.Controllers

@{ Layout = ""; }

    <div class="col left">
        <div class="fieldRow">
            @MvcHtmlString.Create(FormFor<UserController>(x => x.SaveEmail(null)).AddClass("dci_CRUD").ElementId("CRUDForm").ToString())
                <input type="hidden" name="ParentId" value="@Model.ParentId"/>
                @Html.Partial("EmailTemplate", @Model)
            </form>
        </div>
    </div>
 