@using SystemSupport.Web.Controllers
@model AddressViewModel

@{ Layout = ""; }

    <div class="col left">
        <div class="fieldRow">
            @MvcHtmlString.Create(FormFor<UserController>(x => x.SaveAddress(null)).AddClass("dci_CRUD").ElementId("CRUDForm").ToString())
                <input type="hidden" name="ParentId" value="@Model.ParentId"/>
                @Html.Partial("AddressTemplate", @Model)
            </form>
        </div>
    </div>
            
